@model IEnumerable<HOTEL.Models.RoomTypes>

@{
    ViewBag.Title = "Типы комнат";
}

<h2>Типы комнат</h2>

<p>
    @Html.ActionLink("Создать", "Create", null, new { @class = "btn btn-success" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RoomTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RoomTypeName)
        </td>
        <td>
            <div class="btn-group">
                <button type="button" onclick="location.href='@Url.Action("Edit", new { id = item.RoomTypeId })'" class="btn btn-warning"><i class="fa fa-pencil"></i> Редактировать</button>
                <button type="button" onclick="location.href='@Url.Action("Delete", new { id = item.RoomTypeId })'" class="btn btn-danger"><i class="fa fa-trash"></i> Удалить</button>
            </div>
        </td>
    </tr>
}

</table>
